import { RoomsFilterPipe } from './rooms-filter.pipe';

describe('RoomsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RoomsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
